str1=input("enter string")
vow=0
cons=0
vow_char="aeiouAEIOU"
for ch in str1:
    if(ch.isalpha()):
        if ch in vow_char:
            vow+=1
        else:         
            cons+=1
print("Total vowels:", vow)
print("Total cons:",cons)
